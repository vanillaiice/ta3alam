<%= t("mailers.users.invite.hi", name: @user.name) %>

<%= t("mailers.users.invite.body_text", role: @user.role.capitalize) %>

<%= t("mailers.users.invite.instructions") %>:

<%= edit_password_url(@user.password_reset_token) %>

<%= t("mailers.users.invite.footer") %>
